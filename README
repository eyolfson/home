emacs
~/etc/emacs/ -> ~/.emacs.d

git
~/etc/git/config -> ~/.gitconfig

kernel
~/etc/kernel/<KERNEL_VERSION>-config -> /usr/src/<KERNEL_VERSION>/.config

mpd
~/etc/mpd/config -> ~/.mpdconf

portage
~/etc/portage/make.conf -> /etc/make.conf
~/etc/portage/package.keywords -> /etc/portage/package.keywords
~/etc/portage/package.unmask -> /etc/portage/package.unmask
~/etc/portage/package.use -> /etc/portage/package.use
~/etc/portage/world -> /var/lib/portage/world

xmonad
~/etc/xmonad/ -> ~/.xmonad

xorg
~/etc/xorg/fontlist-fix
Python script that finds all installed fonts and runs mkfontdir on those that have not been indexed yet, then generates output to be pasted into /etc/X11/xorg.conf. [http://en.gentoo-wiki.com/wiki/X.Org/Fonts]
~/etc/xorg/resources -> ~/.Xresources
~/etc/xorg/script -> ~/.xinitrc
~/etc/xorg/script -> ~/.xsession
